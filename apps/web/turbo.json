{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": [".svelte-kit/**"]
		},
		"postbuild": {
			"dependsOn": ["build"]
		},
		"preview": {},
		"lint": {}
	}
}
